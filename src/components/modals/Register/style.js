import styled from "styled-components";

export const Form = styled.form`
  display: flex;
  flex-direction: column;
  width: 100%;

  input[type="text"] {
    margin-top: 25px;
  }

  input[type="password"] {
    margin-top: 25px;
  }

  input[type="button"] {
    margin-top: 25px;
  }

  input[type="submit"] {
    margin-top: 25px;
  }
`;
